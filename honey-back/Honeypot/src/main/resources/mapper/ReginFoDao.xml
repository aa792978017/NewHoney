<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--对应接口，写入sql语句-->

<mapper namespace="com.honeypot.honeypot.dao.ReginfoDao">


    <select id="getAllReginfo" resultType="com.honeypot.honeypot.entity.Reginfo">
        SELECT *
        FROM reginfo
        where uniqueId = #{uniqueId}

    </select>

</mapper>